# 本地 AI 行为约束

- 核心业务逻辑禁止写入 ui 层
- 使用 signal_bus 统一管理跨模块信号
- 对于组件组合而成的复杂 UI 结构，必须抽离为 `ui/components` 下的独立组件，页面层禁止直接内联拼装结构
- 页面层在组装业务组件时，必须显式命名组件实例后再加入布局，避免在页面中用循环临时创建匿名核心展示组件
- 积极编写日志，在必要的节点尽可能多写日志
- 必须编写行内注释，使用简体中文，简洁精炼，结构一般为：“谓语 + 宾语”，如“获取当前目录”、“刷新表格数据”、“打开日志目录”、“发射xx信号”、“创建xx线程”等。
