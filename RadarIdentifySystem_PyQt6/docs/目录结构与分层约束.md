# 目录结构与分层约束（基线）

## 1. 目录基线（当前采纳）

```text
easyVer-style (RadarIdentifySystem_PyQt6)
├── .agents/
│   └── rules/
├── app/
│   ├── __init__.py
│   ├── app_config.py
│   ├── application.py
│   ├── events.py
│   ├── logger.py
│   ├── resource_rc.py
│   ├── signal_bus.py
│   └── style_sheet.py
├── config/
│   └── config.json
├── core/
│   ├── models/
│   ├── preprocess.py
│   ├── slicing.py
│   ├── clustering.py
│   ├── recognition.py
│   ├── params_extract.py
│   └── merge.py
├── infra/
│   ├── parsers.py
│   ├── onnx.py
│   ├── plotting.py
│   ├── storage.py
│   └── packaging.py
├── runtime/
│   ├── events.py
│   ├── workflow/
│   └── threading/
├── docs/
├── resources/
├── ui/
│   ├── dialogs/
│   ├── interfaces/
│   ├── components/
│   └── main_window.py
├── utils/
├── main.py
├── pyproject.toml
└── requirements.txt
```

说明：
- 本项目当前阶段不引入 `db` 层。
- 如后续确有落库需求，需单独评审并新增 `db/` 目录与迁移规范。

## 2. 分层职责
1. `app`：生命周期、启动装配、全局日志/配置/信号总线/样式资源管理。
2. `core`：纯业务规则与算法，不依赖 UI/线程/数据库。
3. `infra`：第三方/系统适配（文件解析、推理、导出、绘图、存储）。
4. `runtime`：流程编排、任务调度、线程执行、流程事件模型。
5. `ui`：界面组件与交互，只发起意图并响应结果，不直接承载业务算法。
6. `utils`：无业务语义的通用工具。

## 3. 强制依赖方向
- `ui -> runtime -> core`
- `ui -> app`（仅访问全局能力：配置/日志/样式/信号总线）
- `runtime -> infra`（按需调用适配层）
- `infra -> core`（仅调用 core 暴露能力）
- `core` 禁止依赖 `ui/app/infra/runtime`

## 4. 明确约束
1. `workers` 不得放入 `core`，统一落在 `runtime/threading`。
2. `workflow` 与 `threading` 必须同级并统一落在 `runtime/` 下。
3. 切片/识别/合并等流程事件模型统一落在 `runtime/events.py`。
4. 配置唯一入口为 `app/app_config.py`，持久化文件为 `config/config.json`。
5. 不允许新增第二套配置系统。
6. 不允许在 `ui` 直接调用 `core`/`infra` 具体实现。
7. `ui` 槽函数仅处理界面状态与意图发射，业务处理槽函数必须落在 `runtime/workflow`。
